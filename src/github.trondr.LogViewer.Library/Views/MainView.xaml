<ui:ViewBase x:Class="github.trondr.LogViewer.Library.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:github.trondr.LogViewer.Library.ViewModels"
             xmlns:ui="clr-namespace:github.trondr.LogViewer.Library.Common.UI"
             xmlns:properties="clr-namespace:github.trondr.LogViewer.Library.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="330" d:DesignWidth="700">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TraceColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=TraceColor, Mode=TwoWay}"/>
        <SolidColorBrush x:Key="DebugColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=DebugColor, Mode=TwoWay}"/>
        <SolidColorBrush x:Key="InfoColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=InfoColor, Mode=TwoWay}"/>
        <SolidColorBrush x:Key="WarnColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=WarnColor, Mode=TwoWay}"/>
        <SolidColorBrush x:Key="ErrorColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=ErrorColor, Mode=TwoWay}"/>
        <SolidColorBrush x:Key="FatalColor" Color="{Binding Source={x:Static properties:Settings.Default},Path=FatalColor, Mode=TwoWay}"/>
        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True" />
        </Style>
    </UserControl.Resources>
    
    <DockPanel d:DataContext="{d:DesignInstance Type=viewModels:DesignTimeMainViewModel, IsDesignTimeCreatable=True}" Margin="5" >
        <DockPanel DockPanel.Dock="Right">
            <DockPanel DockPanel.Dock="Top">
                <Button DockPanel.Dock="Right" Content="X" Command="{Binding ClearSearchFilterCommand}" ToolTip="Clear message filter" Margin="2,2,2,2"/>
                <TextBox Text="{Binding SearchFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" ToolTip="Message filter" Margin="2,2,2,2"/>
                
            </DockPanel>
            <TreeView DockPanel.Dock="Top" Width="200" ItemsSource="{Binding LogLevels}" ToolTip="Log level filter" Margin="2,2,2,2">
                <TreeView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsVisible}" Command="{Binding RelativeSource={RelativeSource AncestorType=DockPanel},Path=DataContext.UpdateCommand}">
                                <TextBlock Text="{Binding Level}"/>                                
                            </CheckBox>                            
                        </StackPanel>                        
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <TreeView DockPanel.Dock="Top" Width="200" ItemsSource="{Binding Loggers}" ToolTip="Logger name filter" Margin="2,2,2,2">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type viewModels:LoggerViewModel}">
                        <StackPanel>
                            <CheckBox IsChecked="{Binding IsVisible}" Command="{Binding RelativeSource={RelativeSource AncestorType=DockPanel},Path=DataContext.UpdateCommand}">
                                <TextBlock Text="{Binding DisplayName}"></TextBlock>
                            </CheckBox>
                        </StackPanel>            
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>            
        </DockPanel>
        <ListView ItemsSource="{Binding LogItemsView}" DockPanel.Dock="Top" Margin="2,2,2,2">
            <ListView.Resources>
                <Style TargetType="{x:Type ListViewItem}" >
                    <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
                    <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Trace">
                            <Setter Property="Foreground" Value="{StaticResource TraceColor}"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Debug">
                            <Setter Property="Foreground" Value="{StaticResource DebugColor}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Info">
                            <Setter Property="Foreground" Value="{StaticResource InfoColor}"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Warn">
                            <Setter Property="Foreground" Value="{StaticResource WarnColor}"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Error">
                            <Setter Property="Foreground" Value="{StaticResource ErrorColor}"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=LogLevel.Level}" Value="Fatal">
                            <Setter Property="Foreground" Value="{StaticResource FatalColor}"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="Auto" Header="Time" DisplayMemberBinding="{Binding Path=Time}"/>
                    <GridViewColumn Width="Auto" Header="Level" DisplayMemberBinding="{Binding Path=LogLevel.Level}"/>                    
                    <GridViewColumn Width="Auto" Header="Logger" DisplayMemberBinding="{Binding Path=Logger.Name}"/>
                    <GridViewColumn Width="Auto" Header="Thread" DisplayMemberBinding="{Binding Path=ThreadId}"/>
                    <GridViewColumn Width="Auto" Header="Message" DisplayMemberBinding="{Binding Path=Message}"/>
                </GridView>                
            </ListView.View>
        </ListView>                
    </DockPanel>
</ui:ViewBase>
